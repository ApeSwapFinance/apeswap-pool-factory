{
  "name": "apeswap-pool-factory",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "ganache": "npx ganache-cli",
    "test": "truffle test",
    "compile": "truffle compile",
    "lint": "solhint './contracts/**/*.sol'",
    "migrate:development": "truffle migrate --network development --reset",
    "migrate:testnet": "truffle migrate --network testnet",
    "migrate:bsc": "truffle migrate --network bsc",
    "verify:bsc": "truffle run verify ApeRewardPoolFactoryProxy --network bsc && truffle run verify ApeRewardPoolFactory --network bsc && truffle run verify ApeRewardPool --network bsc",
    "verify:bsc:pool": "truffle run verify ApeRewardPoolFactoryProxy --network bsc && truffle run verify ApeRewardPoolFactory --network bsc",
    "verify:testnet": "truffle run verify ApeRewardPoolFactoryProxy --network testnet && truffle run verify ApeRewardPoolFactory --network testnet && truffle run verify ApeRewardPool --network testnet",
    "verify:testnet:pool": "truffle run verify ApeRewardPoolFactoryProxy --network testnet && truffle run verify ApeRewardPoolFactory --network testnet",
    "verify:pool": "truffle run verify ApeRewardPool --network testnet"
  },
  "author": "DeFiFoFum",
  "license": "ISC",
  "devDependencies": {
    "@openzeppelin/test-helpers": "^0.5.10",
    "dotenv": "^8.2.0",
    "solhint": "^3.3.4",
    "truffle": "^5.2.6",
    "truffle-hdwallet-provider": "^1.0.17",
    "truffle-plugin-verify": "^0.5.7"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.0.0",
    "ethers": "^5.0.32"
  }
}
